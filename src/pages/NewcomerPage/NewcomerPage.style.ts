import { styled } from '@material-ui/core/styles';

import { ValidationMessage as _ValidationMessage } from '~/components/ValidationMessage';

export const ValidationMessage = styled(_ValidationMessage)({
    margin: '0 -16px'
});
