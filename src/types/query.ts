export type QueryOffsetType = {
    limit: number;
    offset: number;
};

export type QuerySortType = 'desc' | 'descending' | 'asc' | 'ascending';

export type QuerySortParamType = { sort: string };
