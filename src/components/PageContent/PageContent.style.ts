import { styled } from '@material-ui/core/styles';

export const PageContent = styled('main')({
    maxWidth: '936px',
    padding: '40px 0',
    margin: '0 auto',
    minHeight: '100%',
    boxSizing: 'border-box'
});
